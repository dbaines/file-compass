<Window x:Class="HDDIndexer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="24"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <Grid Grid.Row="0" Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="4" Margin="8,0">
                <Button x:Name="MenuButton" Style="{StaticResource NavigationBackButtonNormalStyle}">
                    <FontIcon Glyph="&#xE700;" FontSize="16"/>
                </Button>
                <TextBlock Text="HDD Indexer" VerticalAlignment="Center" FontSize="16" FontWeight="SemiBold"/>
            </StackPanel>

            <AutoSuggestBox Grid.Column="1"
                          x:Name="SearchBox"
                          PlaceholderText="Search files..."
                          QueryIcon="Find"
                          MaxWidth="400"
                          Margin="8,8"
                          HorizontalAlignment="Center"
                          QuerySubmitted="SearchBox_QuerySubmitted"
                          TextChanged="SearchBox_TextChanged"/>

            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="4" Margin="8,0">
                <Button x:Name="RefreshButton" ToolTipService.ToolTip="Refresh">
                    <FontIcon Glyph="&#xE72C;" FontSize="16"/>
                </Button>
                <Button x:Name="SettingsButton" ToolTipService.ToolTip="Settings">
                    <FontIcon Glyph="&#xE713;" FontSize="16"/>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Main Content -->
        <NavigationView Grid.Row="1"
                       x:Name="NavigationViewControl"
                       PaneDisplayMode="Left"
                       IsBackButtonVisible="Collapsed"
                       IsSettingsVisible="True"
                       SelectionChanged="NavigationView_SelectionChanged">

            <NavigationView.MenuItems>
                <NavigationViewItem Icon="Home" Content="Dashboard" Tag="Dashboard">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE80F;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem Content="Drives" Tag="Drives">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xEDA2;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem Content="Browse Files" Tag="Browse">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8B7;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem Content="Search" Tag="Search">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE721;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItemSeparator/>

                <NavigationViewItem Content="Backup & Restore" Tag="Backup">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8F1;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem Content="Print & Export" Tag="Print">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE749;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <NavigationView.Content>
                <Grid>
                    <Frame x:Name="ContentFrame" Margin="12"/>
                </Grid>
            </NavigationView.Content>
        </NavigationView>

        <!-- Status Bar -->
        <Grid Grid.Row="2" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                      x:Name="StatusText"
                      Text="Ready"
                      VerticalAlignment="Center"
                      Margin="8,0"
                      FontSize="12"/>

            <TextBlock Grid.Column="1"
                      x:Name="DriveCountText"
                      Text="0 Drives"
                      VerticalAlignment="Center"
                      Margin="8,0"
                      FontSize="12"/>

            <ProgressRing Grid.Column="2"
                         x:Name="StatusProgressRing"
                         IsActive="False"
                         Width="16"
                         Height="16"
                         Margin="8,0"/>
        </Grid>
    </Grid>
</Window>