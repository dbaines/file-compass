<Page x:Class="HDDIndexer.Views.BackupRestoreView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Backup & Restore" FontSize="24" FontWeight="Bold"/>
            <TextBlock Text="Backup your catalog database and restore from previous backups" FontSize="14" Opacity="0.8"/>
        </StackPanel>

        <!-- Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Backup Section -->
            <Border Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="20">
                <StackPanel Spacing="15">
                    <TextBlock Text="Create Backup" FontSize="18" FontWeight="SemiBold"/>
                    <TextBlock Text="Create a backup of your catalog database" Opacity="0.8"/>

                    <Button Content="Create Full Backup" HorizontalAlignment="Stretch" Style="{StaticResource AccentButtonStyle}"/>
                    <Button Content="Create Incremental Backup" HorizontalAlignment="Stretch"/>
                    <Button Content="Schedule Automatic Backups" HorizontalAlignment="Stretch"/>
                </StackPanel>
            </Border>

            <!-- Restore Section -->
            <Border Grid.Column="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="20">
                <StackPanel Spacing="15">
                    <TextBlock Text="Restore Backup" FontSize="18" FontWeight="SemiBold"/>
                    <TextBlock Text="Restore your catalog from a backup file" Opacity="0.8"/>

                    <Button Content="Select Backup File" HorizontalAlignment="Stretch"/>
                    <Button Content="Validate Backup" HorizontalAlignment="Stretch"/>
                    <Button Content="Restore Database" HorizontalAlignment="Stretch" Style="{StaticResource AccentButtonStyle}"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>