<Page x:Class="HDDIndexer.Views.DashboardView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">

    <Grid>
        <ScrollViewer>
            <StackPanel Spacing="20" Margin="20">
                <!-- Welcome Header -->
                <TextBlock Text="Welcome to HDD Indexer" FontSize="24" FontWeight="Bold"/>
                <TextBlock Text="Your comprehensive file catalog manager for Windows 11" FontSize="14" Opacity="0.8"/>

                <!-- Quick Stats -->
                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        CornerRadius="8" Padding="20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                            <FontIcon Glyph="&#xEDA2;" FontSize="32" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                            <TextBlock Text="0" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock Text="Cataloged Drives" FontSize="12" HorizontalAlignment="Center" Opacity="0.8"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                            <FontIcon Glyph="&#xE8B7;" FontSize="32" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                            <TextBlock Text="0" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock Text="Total Files" FontSize="12" HorizontalAlignment="Center" Opacity="0.8"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                            <FontIcon Glyph="&#xE8F1;" FontSize="32" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                            <TextBlock Text="0 GB" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock Text="Total Size" FontSize="12" HorizontalAlignment="Center" Opacity="0.8"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Quick Actions -->
                <TextBlock Text="Quick Actions" FontSize="18" FontWeight="SemiBold"/>
                <UniformGrid Columns="2" HorizontalAlignment="Stretch">
                    <Button Content="Add New Drive"
                            HorizontalAlignment="Stretch"
                            Margin="0,0,10,0"
                            Padding="20"
                            Style="{StaticResource AccentButtonStyle}">
                        <Button.Content>
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE710;" FontSize="16"/>
                                <TextBlock Text="Add New Drive"/>
                            </StackPanel>
                        </Button.Content>
                    </Button>

                    <Button Content="Start Quick Scan"
                            HorizontalAlignment="Stretch"
                            Margin="10,0,0,0"
                            Padding="20">
                        <Button.Content>
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE72C;" FontSize="16"/>
                                <TextBlock Text="Start Quick Scan"/>
                            </StackPanel>
                        </Button.Content>
                    </Button>
                </UniformGrid>

                <!-- Recent Activity -->
                <TextBlock Text="Recent Activity" FontSize="18" FontWeight="SemiBold"/>
                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        CornerRadius="8" Padding="20">
                    <TextBlock Text="No recent activity" Opacity="0.6" HorizontalAlignment="Center"/>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>