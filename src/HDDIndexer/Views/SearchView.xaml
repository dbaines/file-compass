<Page x:Class="HDDIndexer.Views.SearchView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:models="using:HDDIndexer.Models"
      xmlns:converters="using:HDDIndexer.Converters"
      mc:Ignorable="d">

    <Page.Resources>
        <converters:FileSizeConverter x:Key="FileSizeConverter"/>
        <converters:DateTimeConverter x:Key="DateTimeConverter"/>
        <converters:CountToVisibilityConverter x:Key="CountToVisibilityConverter"/>
        <converters:InverseCountToVisibilityConverter x:Key="InverseCountToVisibilityConverter"/>
        <converters:FileIconConverter x:Key="FileIconConverter"/>
    </Page.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Search Files" FontSize="24" FontWeight="Bold"/>
            <TextBlock Text="Find files across all your cataloged drives" FontSize="14" Opacity="0.8"/>
        </StackPanel>

        <!-- Search Box -->
        <Border Grid.Row="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="8" Padding="20" Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                        Text="{x:Bind ViewModel.SearchQuery, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        PlaceholderText="Enter filename to search..."
                        Margin="0,0,10,0"/>
                <Button Grid.Column="1"
                       Content="Search"
                       Command="{x:Bind ViewModel.PerformQuickSearchCommand}"
                       Style="{StaticResource AccentButtonStyle}"
                       Margin="0,0,10,0"/>
                <Button Grid.Column="2"
                       Content="Advanced"
                       Command="{x:Bind ViewModel.ToggleAdvancedSearchCommand}"
                       Margin="0"/>
            </Grid>
        </Border>

        <!-- Results -->
        <Border Grid.Row="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Results Header -->
                <Border Grid.Row="0" Padding="20,15,20,10" Visibility="{x:Bind ViewModel.SearchResults.Count, Mode=OneWay, Converter={StaticResource CountToVisibilityConverter}}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{x:Bind ViewModel.SearchStatus, Mode=OneWay}" FontWeight="SemiBold"/>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="10">
                            <Button Content="Export PDF" Command="{x:Bind ViewModel.ExportResultsCommand}" CommandParameter="pdf" FontSize="12" Padding="8,4"/>
                            <Button Content="Export Excel" Command="{x:Bind ViewModel.ExportResultsCommand}" CommandParameter="excel" FontSize="12" Padding="8,4"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Results List -->
                <ListView Grid.Row="1"
                         ItemsSource="{x:Bind ViewModel.SearchResults, Mode=OneWay}"
                         Margin="10"
                         Visibility="{x:Bind ViewModel.SearchResults.Count, Mode=OneWay, Converter={StaticResource CountToVisibilityConverter}}">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:FileEntry">
                            <Grid Padding="10,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <FontIcon Grid.Column="0" Glyph="{x:Bind FileExtension, Converter={StaticResource FileIconConverter}}" FontSize="16" Margin="0,0,10,0"/>
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{x:Bind FileName}" FontWeight="SemiBold"/>
                                    <TextBlock Text="{x:Bind FilePath}" FontSize="11" Opacity="0.6"/>
                                </StackPanel>
                                <TextBlock Grid.Column="2" Text="{x:Bind FileSize, Converter={StaticResource FileSizeConverter}}" FontSize="12" Margin="10,0"/>
                                <TextBlock Grid.Column="3" Text="{x:Bind DateModified, Converter={StaticResource DateTimeConverter}}" FontSize="12" Margin="10,0"/>
                                <TextBlock Grid.Column="4" Text="{x:Bind Drive.DriveName}" FontSize="12" Opacity="0.8" Margin="10,0"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <!-- Empty State -->
                <TextBlock Grid.Row="1"
                          Text="Enter a search term to find files"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"
                          Opacity="0.6"
                          Visibility="{x:Bind ViewModel.SearchResults.Count, Mode=OneWay, Converter={StaticResource InverseCountToVisibilityConverter}}"/>

                <!-- Loading Indicator -->
                <StackPanel Grid.Row="1"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Spacing="10"
                           Visibility="{x:Bind ViewModel.IsBusy, Mode=OneWay}">
                    <ProgressRing IsActive="{x:Bind ViewModel.IsBusy, Mode=OneWay}" Width="32" Height="32"/>
                    <TextBlock Text="Searching..." Opacity="0.8"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>