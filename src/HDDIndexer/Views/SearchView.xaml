<Page x:Class="HDDIndexer.Views.SearchView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Search Files" FontSize="24" FontWeight="Bold"/>
            <TextBlock Text="Find files across all your cataloged drives" FontSize="14" Opacity="0.8"/>
        </StackPanel>

        <!-- Search Box -->
        <Border Grid.Row="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="8" Padding="20" Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" PlaceholderText="Enter filename to search..." Margin="0,0,10,0"/>
                <Button Grid.Column="1" Content="Search" Style="{StaticResource AccentButtonStyle}" Margin="0,0,10,0"/>
                <Button Grid.Column="2" Content="Advanced" Margin="0"/>
            </Grid>
        </Border>

        <!-- Results -->
        <Border Grid.Row="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8">
            <Grid>
                <TextBlock Text="Enter a search term to find files"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"
                          Opacity="0.6"/>
            </Grid>
        </Border>
    </Grid>
</Page>